# Iterative Fibonacci: fib(15) = 610, printed via println(I).
# Positive corpus file: must parse, verify, and run under T0 with the exact
# expected stdout (Java semantics oracle, Rule 67).
#
# Rule 88 (poll safepoints at loop backedges) is honored by placing a
# safepoint_poll at the loop head (form (a) of the baseline contract: the
# destination of the backward goto IS a safepoint_poll). This matches the
# frontend lowering convention in compiler/frontend/src/LowerStmt.cpp
# (lowerWhile/lowerDoWhile/lowerFor all emit Op::SafepointPoll immediately
# after the loop-head label) so that hand-written corpus programs and
# b2parse --emit-rbc output are T1-plannable under the same rule. See
# MSG-20260918-002 (now CLOSED) for the audit trail.

.class Main

.method static main ()V
.regs 6
.locals 3
.const c0 = field java/lang/System out Ljava/io/PrintStream;
.const c1 = method java/io/PrintStream println (I)V
iconst r0 0
istore r0 l0
iconst r0 1
istore r0 l1
iconst r0 15
istore r0 l2
Lloop:
safepoint_poll
iload r0 l2
ifle r0 Ldone
iload r0 l0
iload r1 l1
iadd r2 r0 r1
iload r0 l1
istore r0 l0
istore r2 l1
iload r3 l2
iinc r3 -1
istore r3 l2
goto Lloop
Ldone:
getstatic r0 c0
iload r1 l0
invokevirtual r2 r0 r2 c1
return
.end
